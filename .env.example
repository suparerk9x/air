# Database — PostgreSQL connection string
DATABASE_URL="postgresql://postgres:password@localhost:5432/air?schema=public"

# Session — generate with: openssl rand -base64 32
SESSION_SECRET="change-me-to-a-random-secret"
